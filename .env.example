# ─────────────────────────────────────────────────────────────────────────────
# Variables de entorno del SERVIDOR (Vercel → Project Settings → Environment Variables).
# NO son del cliente (Vite): NUNCA uses el prefijo VITE_ para estas ni las metas al
# bundle. La service_role da acceso total a la base.
#
# Requeridas por api/complete-password-change.mjs (CAMBIO 3):
# ─────────────────────────────────────────────────────────────────────────────

# URL del proyecto Supabase (simoOS-prod)
SUPABASE_URL=https://eykplgdwlqpybzkzbpmu.supabase.co

# service_role key del proyecto (SECRETO). Mismo nombre que en simo-sync:
# un solo nombre para la llave, para no romper la proxima rotacion.
SUPABASE_SERVICE_ROLE=
